# Baked into `next build` (public; never put secrets in NEXT_PUBLIC_*).
# `next dev` does not load this file — use `.env.development` and `.env.local`.
NEXT_PUBLIC_API_URL=https://api.tunzone.com/api
NEXT_PUBLIC_SITE_URL=https://published.tunzone.com
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-NEC36L9NYL
